#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
const char *text;
int count;
text = "I'm not going anywhere. You can print that wherever you want to.
I'm here and I'm a Spur for life";
count = _printf("%s %c %s %% %c\n", "Hello", 'W', text, '!');
printf("\nTotal characters printed: %d\n", count);
return (0);
}
